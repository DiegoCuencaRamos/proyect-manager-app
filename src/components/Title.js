import React from 'react'

const Title = ({ title, description }) => (
    <div>
        <div>
            <h1>{title}</h1>
            <p>{description}</p>
        </div>
    </div>
)

export default Title